# Backend origin used by the production build.
#
# REST keeps going through the same-origin /api rewrite in vercel.json, but the
# WebSocket cannot: Vercel does not proxy an upgrade request, so the live feed
# has to dial the backend directly.
VITE_WS_BASE=https://nextvolt-19eo.onrender.com
